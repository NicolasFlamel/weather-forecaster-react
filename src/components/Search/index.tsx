import './styles.css';

const Search = () => {
  return (
    <search className="weather-search" role="search">
      <form>
        <h1>Search</h1>
      </form>
    </search>
  );
};

export default Search;
